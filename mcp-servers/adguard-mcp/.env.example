# AdGuard Home MCP Server — Environment Variables
# Copy to .env and fill in your values

# AdGuard Home instance URL (no trailing slash)
# For HA setup: use primary instance URL
ADGUARD_URL=http://localhost:3053

# AdGuard Home admin credentials
ADGUARD_USER=admin
ADGUARD_PASSWORD=your_password_here
